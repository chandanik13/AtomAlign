{
  "name": "atomalign",
  "version": "1.0.0",
  "description": "Atomberg Goal Setting & Tracking Portal",
  "scripts": {
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm run dev",
    "seed": "cd backend && npm run seed",
    "install:all": "cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": [
    "atomberg",
    "goal-tracking",
    "hrms"
  ],
  "author": "Atomberg",
  "dependencies": {
    "dotenv": "^17.4.2"
  }
}
